version: "v1alpha1"
attenuation:
  policies:
    - 'allow if service("system", "/sam/mcp/1.0.0"), node($peer)'
services:
  - type: "mcp"
    name: "code-reviewer"
    description: "Reviews code snippets via a Gemini-backed agent"
    target_url: "http://127.0.0.1:7779/mcp"
