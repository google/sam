name: sam_agent
description: "SAM Mobile Agent - Secure, decentralized mesh node and MCP tool registry for Android and iOS."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  path_provider: ^2.1.1
  http: ^1.2.0
  url_launcher: ^6.2.5
  crypto: ^3.0.3
  qr_flutter: ^4.1.0
  firebase_core: ^4.0.0
  firebase_ai: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
